select * from employee.employee order by age desc limit 5;
select * from employee.employee order by age desc limit 2,1;

select * from world.city order by Name limit 10;
select * from world.city order by Name limit 8,5;

-- aliases
-- SQL aliases are used to give a table, or a column in a table, a temporary name.  
